package com.jbt.service.impl;import java.util.List;import javax.annotation.Resource;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import org.springframework.stereotype.Service;import com.jbt.bean.CrawlInfo;import com.jbt.dao.CrawlInfoMapper;import com.jbt.service.CrawlInfoService;import com.jbt.util.Page;import com.jbt.util.SessionUtil;@Service("CrawlInfoService")public class CrawlInfoServiceImpl implements CrawlInfoService {	private final Logger log = LoggerFactory.getLogger(CrawlInfoServiceImpl.class);	@Resource	private CrawlInfoMapper CrawlInfoMapper;	@Override	public int insert(CrawlInfo crawlInfo) {		// TODO Auto-generated method stub		return this.CrawlInfoMapper.insert(crawlInfo);	}	@Override	public Page getCrawlList(CrawlInfo crawlInfo, Integer pageNow) {		int start = (pageNow - 1) * Page.PSize;		Integer id = SessionUtil.getManagerId();		List<CrawlInfo> list = this.CrawlInfoMapper.getCrawlList(crawlInfo, start, Page.PSize, id);		int count = this.CrawlInfoMapper.getCrawlCount(crawlInfo, id);		return new Page(list, pageNow, count);	}}